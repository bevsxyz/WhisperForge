Run the test suite (see CLAUDE.md § Commands for rationale on flags):

```bash
cargo test --release -p whisperforge-core -p whisperforge-convert -p whisperforge-cli
```

Report pass/fail count and full output for any failures.